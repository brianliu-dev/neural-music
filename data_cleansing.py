import numpy as np
import os

file_path = "./data/large_dataset.txt"

# Mostly pseudocode for now

start = 0
end = 107
notes = np.array(150000)[104]

# Parse, remove blemishes
def parse_line(line):
	# do stuff
	return line
	
conversion_table = {}

def convert_to_ascii(line):
	# convert to ascii
	return line

def inverse_conversion():
	# Need to convert back to midi format!
	

# Read in data from file
with open(file_path) as f:
    parse_line(f.readline())
    count+=1

